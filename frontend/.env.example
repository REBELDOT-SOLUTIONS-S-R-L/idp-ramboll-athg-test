# Base URL of the Rust backend, read at runtime by the server functions.
# Local development default is http://localhost:3001.
# In Docker Compose, the backend is reachable on the internal network at http://backend:3001.
BACKEND_URL=http://localhost:3001
